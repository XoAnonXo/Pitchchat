# Pitchchat Environment Variables Example
# Copy this file to .env and fill in the values

# Server Port (defaults to 5000 if not set)
PORT=5000

# PostgreSQL connection string
DATABASE_URL=postgres://user:password@localhost:5432/pitchchat
# Optional pooled connection string (e.g., Neon pooler)
DATABASE_URL_POOLER=postgres://user:password@localhost:5432/pitchchat?sslmode=require

# Secret for session management (generate a strong random string)
SESSION_SECRET=your_session_secret_here
# Optional session store tuning
# SESSION_STORE=postgres # or memory (defaults to memory in development)
# SESSION_DISABLE_TOUCH=1
# SESSION_PRUNE_INTERVAL_SECONDS=900

# Encryption key for third-party credentials (must be exactly 32 characters)
ENCRYPTION_KEY=32_character_long_secret_key_!!!

# Optional timing diagnostics
# LOG_TIMINGS=1
# LOG_DB_QUERIES=1

# Stripe Configuration
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
# Optional Stripe sync on auth (disabled by default)
# STRIPE_SYNC_ON_AUTH=1
# STRIPE_SYNC_TTL_SECONDS=900

# Brevo (SendinBlue) API Key
BREVO_API_KEY=xkeysib-...

# AI API Keys
OPENAI_API_KEY="your-openai-api-key"

# Google OAuth
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Google OAuth Configuration
# The above placeholders are more explicit, but these are the original ones if you prefer
# GOOGLE_CLIENT_ID=...apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=...

# Production URL (for email links and OAuth callbacks)
PRODUCTION_URL=https://pitchchat.ai
# Public base URL for server-generated links (optional)
# PUBLIC_BASE_URL=https://pitchchat.ai

# Internal API key for protected internal endpoints (cron/email jobs)
# INTERNAL_API_KEY=your_internal_key_here

# Optional: Replit Specifics
REPLIT_DOMAINS=your-repl-name.user.repl.co,pitchchat.ai
ISSUER_URL=https://replit.com/oidc
REPL_ID=...

# Optional: AI API Keys (if not using shared enterprise keys)
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
GOOGLE_GENAI_API_KEY=...
